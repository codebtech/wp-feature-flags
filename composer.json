{
    "name": "mr/feature-flags",
    "description": "Allows developers to enable / disable features based on flags.",
    "type": "wordpress-plugin",
    "license": "proprietary",
    "require-dev": {
        "phpunit/phpunit": "^9.4",
        "brain/monkey": "^2.6",
        "newsuk/nuk-wp-phpcs-config": "^0.1.0"
        },
    "autoload": {
        "psr-4": {
            "MR\\FeatureFlags\\": ["includes/"]
        }
    },
    "autoload-dev": {
        "psr-4": {
            "MR\\FeatureFlags\\Tests\\": "tests/unit/"
        }
    },
	"config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    },
    "scripts": {
        "lint": "phpcs .",
        "lint:fix": "phpcbf .",
        "test": "phpunit --dont-report-useless-tests --configuration ./phpunit.xml --testdox"
    }
}
